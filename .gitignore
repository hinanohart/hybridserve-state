# Rust
/target
**/target
# Cargo.lock is committed: this workspace ships a CLI + PyO3 extension, and the
# bitwise rehydration-equivalence claim benefits from pinned, reproducible deps.

# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.so
.pytest_cache/
.ruff_cache/

# Build / test artifacts
*.hss
*.npz

# Autonomous-build local state (not part of the project)
.hybridserve-state-progress.json
experiments/_wip/
